# Backend API URL. Dev: proxy uses this. Prod build: set to your backend (e.g. https://your-backend.onrender.com) so API calls don't 404.
VITE_API_URL=http://localhost:5000

# Google OAuth client ID (same as backend GOOGLE_CLIENT_ID; add your site to Authorized JavaScript origins)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
